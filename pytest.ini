[pytest]
# Only the repo's own tooling tests run here. Tests under examples/ and tasks/
# belong to task bundles: they are verifier code that runs inside a task image
# against a library that does not exist on the host.
testpaths = scripts/tests
norecursedirs = examples tasks templates .git
addopts = -q
