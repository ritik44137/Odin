[metadata]
name = "Total-function page-range parsing for rangespec"
working_slug = "rangespec-total-parse"
collection_family = "Library clone"
task_family = "debugging"
verifier_family = "programmatic"

[agent]
network_mode = "no-network"
timeout_sec = 7200

[verifier]
network_mode = "no-network"
timeout_sec = 900

[environment]
cpus = 2
memory_mb = 2048
storage_mb = 2048
gpus = 0
network_mode = "no-network"
workdir = "/app"
